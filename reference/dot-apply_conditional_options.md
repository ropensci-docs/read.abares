# Apply conditional options to httr2 request

Apply conditional options to httr2 request

## Usage

``` r
.apply_conditional_options(req, url)
```

## Arguments

- req:

  An httr2 request object

- url:

  The URL being requested

## Value

Modified httr2 request object
